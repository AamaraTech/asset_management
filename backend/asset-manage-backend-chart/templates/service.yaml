apiVersion: v1
kind: Service
metadata:
  name: {{ include "asset-manage-backend-chart.fullname" . }}
  labels:
    {{- include "asset-manage-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asset-manage-backend-chart.selectorLabels" . | nindent 4 }}
